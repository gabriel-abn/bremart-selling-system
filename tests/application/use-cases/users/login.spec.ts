describe("Login use case", () => {
  it("should throw if user email does not exist", () => {});
  it("should throw if user email is not verified", () => {});
  it("should throw if user password does not match", () => {});
  it("should throw if user is not active", () => {});
  it("should return an access token", () => {});
});
