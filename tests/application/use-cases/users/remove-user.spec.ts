import { describe, it } from "vitest";

describe("Use Case: Remove User", () => {
  it("should throw an error if the user does not exist");
  it("should remove the user and return the removed user");
});
