describe("Verify email", () => {
  it("should throw if email not found");
  it("should throw if verification token is invalid");
  it("should throw if verification token is expired");
  it("should throw if user is already verified");
  it("should verify user");
});
